# Base URL of the trading-api backend (apps/api). Must be reachable from
# the browser, not just the Next.js server, since every data fetch here
# runs client-side.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
